import React from 'react';
import GasStationForm from './GasStationForm'; // Import the form component
import './App.css'; // Optional: Import global styles, if necessary

const App = () => {
  return (
    <div className="App">
      <main>
        <GasStationForm /> 
      </main>
    </div>
  );
};

export default App;
